# ==================================
# 核心算法与数据处理 (Core ML & Data)
# ==================================
scikit-learn==1.7.2   # 随机森林算法
pandas==2.3.3         # 脑电数据处理 (CSV读写)
numpy==2.3.5          # 数值计算
joblib==1.5.2         # 模型保存与加载 (.pkl)
scipy==1.16.3         # 科学计算 (sklearn的依赖)

# ==================================
# 硬件通信与网络 (Hardware & Network)
# ==================================
pyserial==3.5         # 读取脑电接收器 (COM口)
requests==2.32.5      # 网络请求库
PyYAML==6.0.3         # 配置文件读取 (如果有用到yml配置)

# ==================================
# 基础依赖库 (Dependencies)
# (以下通常不需要手动调用，是上述库自动安装的)
# ==================================
# 时间与日期处理
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2
iso8601==2.1.0

# 网络底层依赖
urllib3==2.5.0
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11

# 兼容性与构建工具
six==1.17.0
future==1.0.0
wheel==0.45.1
setuptools==80.9.0
threadpoolctl==3.6.0